/**
 * Interface {@code CanWrite} specifies that a class can write data from an
 * array of {@code ubyte} values.  This operation is usually applied to
 * files, streams, network connections, and similar I/O abstractions. 
 */
interface shadow:io@CanWrite
{
	/**
	 * Method called to write data.
	 * @param buffer array of {@code ubyte} values to write data from
	 * @return number of {@code ubyte} values successfully written 
	 */
	write( ubyte[] buffer ) => ( long );
}
