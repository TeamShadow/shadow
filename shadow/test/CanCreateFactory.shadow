import shadow:io@Console;

class shadow:test@CanCreateFactory<T is CanCreate>
{
	public generate() => (T)
	{
		//Console.printLine(T:class);
		T value = T:create();
		//Console.printLine("Fantastic!");
		return value;
	}
}