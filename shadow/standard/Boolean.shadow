locked immutable class shadow:standard@Boolean
implements CanEqual<boolean>
{
	public not() => ( boolean )
	{
		return this ? false, true;
	}
	public toString() => ( String )
	{
		return this ? "true", "false";
	}
	
	public equal(boolean other) => (boolean)
	{
		return this === other;	
	}
}