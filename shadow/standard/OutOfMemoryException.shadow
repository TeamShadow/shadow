/**
 * Exception {@code OutOfMemoryException} is thrown when the run time system
 * attempts to allocate memory on the heap and fails because the heap space
 * allotted to the program is exhausted.
 */
exception shadow:standard@OutOfMemoryException
{		
}
