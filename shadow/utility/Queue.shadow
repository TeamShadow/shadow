interface shadow.utility@Queue<E> extends CanIterate<E>
{
	immutable get size() => ( int size );
	add( E element ) => ( boolean success );
	remove() => ( E element, boolean success );
	clear() => ();
}
