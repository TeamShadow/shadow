class If {
    public blah() => () {
        int a = 1;
        int b = 7;
        int c = 3, d = 4;
        int x;

        if(a == b)
        	skip;        
        
        if( a > b )
        	skip;
        
        if( a < b and c < d )
        	x = 10;
        	
        if( a < b or !(c < d) )
        	x = 10;
    }
}
