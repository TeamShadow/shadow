class Index2<E> implements CanIndexStore<code, E>
{

	nullable E storage = null;

	public index(code key) => (E value)
	{		
		return check(storage);
	}
	
	
	public index(code key, E value) => ()
	{
		storage = value;	
	}
}