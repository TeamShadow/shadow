class Scope {

	int a;

    public blah(int a) => () {
        int a = 1;
        int b = 7;
        int c = 3, d = 4;
        int x;
        
        //int a; //uncomment to see multiply defined symbol

        if(a == b)
        	skip;        
        
        if( a > b )
        	skip;
        
        if( a < b and c < d )
        	x = 10;
        	
        if( a < b or !(c < d) )
        	x = 10;
    }
}
