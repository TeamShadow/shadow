attribute Attribute1 {
    double required;
    int optional;

    create(double required)
    {
        this(required, 4);
    }

    create(double required, int optional)
    {
        this:required = required;
        this:optional = optional;
    }
}