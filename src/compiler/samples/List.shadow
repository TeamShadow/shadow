interface List<T> extends AuditableCollection<T>
{
	error IndexOutOfBoundsError;

	immutable get( int index ) => (T) throws IndexOutOfBoundsError;
	set( int index, T value, ) => () throws IndexOutOfBoundsError;
}