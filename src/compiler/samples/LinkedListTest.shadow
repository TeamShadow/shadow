
class LinkedListTest {

	public static main(String[] args) => () {
		var list1 = new LinkedList<int>();
		var list2 = new LinkedList<int>({ 1, 2, 3, 4, 5}); // can I do this? Array is a collection, right?
		
		foreach(int i in list2) {
			list1.add(i);
		}
		
		list1.addAll(list2);
		
		list1.addAll(list1);	// what happens here???
		
		//
		// so how do I average all the numbers in my list?
		// the only way I see to do it is iterate over them or use foreach
		//
		
		
	}
}