interface Collection<T> extends Iterable<T>
{
	constructor( Collection<S is T> collection );
	contains( T value ) => (boolean);
	containsAll( Collection<S is T> collection ) => (boolean);
	isEmpty() => (boolean);
	immutable getSize() => (int);
	immutable toArray() => (T[]);
}