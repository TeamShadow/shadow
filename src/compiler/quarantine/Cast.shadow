class Cast
{
	public immutable compare(Cast other) => (int);
	public works(Object other) => (int)
	{
		return (cast<Cast>(other)).compare(this);
	}
	public fails(Object other) => (int)
	{
		return cast<Cast>(other).compare(this);
	}
}
