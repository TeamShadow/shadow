class Generics
{
	public start(String[] args) => () {
        Pig<String> pig = Pig<String>:construct();
        
        pig.setValue("Help");
        
        nullable String output = pig.getValue();
        nullable Object thing;
        thing = pig.getValue();
        
        GenericMethod object = GenericMethod:construct();
        int x = object.method<int>(13);
        
        Object stuff = "walnuts";
        String value = object.method<String>(stuff);
        
        Child child = Child:construct();
        Thumper<Child, Child> thumper = Thumper<Child, Child>:construct(child);
        
        try
        {        
        	child = check(thumper.thump());
        }
        recover
        {}
    }
}