class Foreach {
    public test() => () {
        foreach(int i in int_list) { ; }
        
        // nested loops
        foreach(int i in int_list) {
            foreach(int i in int_list) {
                foreach(int i in int_list) {
                }
            }
        }
    }
}
