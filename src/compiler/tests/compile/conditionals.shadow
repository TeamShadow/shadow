class Foo {
    public blah() => () {
    
        int a;
        int b;
        float c;
        
        boolean x, y;
        
        if(a < b and c < b);
    
    	if(x or y );
    	
    	if(x xor y );    	
    }
}