class If {
    public blah() => () {
        int a;
        int b = 7;
        int c, d;
        int x;

        if(a == b)
        	skip;        
        
        if( a > b )
        	skip;
        
        if( a < b and c < d )
        	x = 10;
        	
        if( a < b or !(c < d) )
        	x = 10;
    }
}
