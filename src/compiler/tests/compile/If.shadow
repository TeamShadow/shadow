class If {
    public blah() => () {
        int a;
        int b = 7;

        if(a == b);        
        
        if( a > b );
        
        if( a < b and c < d )
        	x = 10;
        	
        if( a < b or !(c < d) )
        	x = 10;
    }
}
