import shadow.utility@Random;

class Imports
{
	public start(String[] args ) => ()
	{
		int a = 10;
		
		Random r = new Random();
		
		a += r.nextInt();
		
		shadow.utility@Random v = new shadow.utility@Random();
		
		a *= v.nextInt();
					
	}
}