class ComplexAccess {

	get set nullable Inner i;

	private class Inner 
	{	
	
		get nullable ComplexAccess o;
	
		public jam(int a) => (int) {
	        return 4;
	    }
	}
	
	private stuff() => (Inner)
	{
		Inner i = Inner:construct();
		return i;
	}
	    
    public start() => ()
    {
    	int c;
    	
    	ComplexAccess[] first = ComplexAccess[100];
    	first[0] = ComplexAccess:construct();
    	
    	try
    	{
    		c = check(first[0]).stuff().jam(10);    	
    		ComplexAccess outer = ComplexAccess:construct();    	
    		check(check(outer->i)->o)->i = Inner:construct();    		
    	}
    	recover
    	{}
    }
}