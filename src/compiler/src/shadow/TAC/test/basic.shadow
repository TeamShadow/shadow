class Bill {

/*
    public int t;
    public int a = 1, y, z = 2;
    public double b = 0.2 + 0.3 + 0.5, g = 1.0 + 5.0;
    public String c = "hello" + "world";
    public boolean x = true;
*/
    
    public test() => () {
        int a = 1 + (2 + 3) + (5 * 8);
        int b = 2;
        
        if(a > b and b < a and a < b)        
        a = a + b;
        b += a;
    }
	
/*    public blah() => () {
        int x, y, z;
        
        if(x > y or z > y) { x = 1; }
        else if(y == x) { y = 1 * 3; }
        
        
        z = 1;
        y = 2;
        
        if(x > y and z < y or z > y) { x += 2; }
        else { y += 1; }
        else if(b > a) { ; }
                
    	while(a == b or a > b) {
    		;
    	}
    	
    	for(int i = 0; i < 12; i += 1) { ; }
    	
    	switch(a) {
    	    case 1: break;
    	       
    	    case 2: break;
        
            default: break;
        }
*/

} 
