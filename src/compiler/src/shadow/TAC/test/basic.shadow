class Bill {
	
    public blah() => () {
        int x, y, z;
        
        if(x > y or z > y) { x = 1; }
        else if(y == x) { y = 1; }
        
        
        z = 1;
        y = 2;
        
        if(x > y and z < y or z > y) { x += 2; }
        else { y += 1; }

/*        else if(b > a) { ; }
                
    	while(a == b or a > b) {
    		;
    	}
    	
    	for(int i = 0; i < 12; i += 1) { ; }
    	
    	switch(a) {
    	    case 1: break;
    	       
    	    case 2: break;
        
            default: break;
        }
*/

    }

} 
