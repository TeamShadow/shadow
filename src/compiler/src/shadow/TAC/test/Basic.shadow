class Basic extends Loops implements TestInterface {

    private int a;
    private int b = 0;

    public thrower() => () throws Basic {
        throw new Basic();
    }
    
    public test() => () {
        try {
            thrower();
        } catch(Basic b) {
            int i;
            i += 2;
        }
    }
    
} 
