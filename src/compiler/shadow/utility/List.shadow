interface shadow.utility@List<E> extends CanIterate<E>, CanIndex<int, E>
{
	immutable get size() => ( int size );
	immutable indexOf(E element) => ( int index );
	immutable contains(E element) => ( boolean found );
	add(E element) => ( boolean success );
	delete(int index) => ( E element );
	remove(E element) => ( boolean success );
	clear() => ();
}
