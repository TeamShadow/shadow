interface shadow.utility@Set<E> extends CanIterate<E>, CanIndex<E, boolean>
{
	readonly get size() => ( int size );
	readonly contains( E element ) => ( boolean found );
	add( E element ) => ( boolean success );
	remove( E element ) => ( boolean success );
	clear() => ();
}
