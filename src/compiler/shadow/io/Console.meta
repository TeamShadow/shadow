import shadow.standard@String;
import shadow.standard@Object;

immutable singleton shadow.io@Console extends shadow.standard@Object
{
	public immutable printLine(nullable Object value) => (Console);
	public immutable printLine() => (Console);
	public create();
	public immutable print(nullable Object value) => (Console);

}
