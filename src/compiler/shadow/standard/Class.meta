import shadow.standard@String;
import shadow.standard@Object;

final immutable class shadow.standard@Class extends shadow.standard@Object
{
	public immutable getSuperClass() => (nullable Class);
	public immutable isSubtype(Class other) => (boolean);
	public immutable isInterface() => (boolean);
	public immutable getClassName() => (String);
	public immutable toString() => (String);

}

