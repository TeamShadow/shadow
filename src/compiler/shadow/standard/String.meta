import shadow.standard@Object;

final immutable class shadow.standard@String extends shadow.standard@Object
{
	public immutable equals(String other) => (boolean);
	public immutable toString() => (String);
	public immutable getCode(long index) => (ubyte);
	public immutable getLength() => (long);

}

