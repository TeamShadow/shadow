class shadow.standard@Object
{
	public native immutable
	getClass() => ( Class );

	public native
	copy() => ( Object ); //classes should override this if needed

	public native
	freeze() => ( immutable Object ); //classes should override this if needed

	public readonly
	equals( readonly Object other ) => ( boolean ) { return this === other; }

	public readonly
	toString() => ( String ) { return getClass().toString(); }
}
