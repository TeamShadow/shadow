final immutable class shadow.standard@UShort implements Number<ushort>, CanCompare<ushort>
{	
	public native immutable add(ushort other) => (ushort);
	public native immutable subtract(ushort other) => (ushort);
	public native immutable multiply(ushort other) => (ushort);
	public native immutable divide(ushort other) => (ushort);
	public native immutable modulus(ushort other) => (ushort);
	
	public native immutable compare(ushort other ) => (int);
}